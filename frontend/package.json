{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node -e \"require('fs').writeFileSync('dist/_headers', '/assets/*\\n  Cache-Control: public, max-age=31536000, immutable\\n\\n/index.html\\n  Cache-Control: public, max-age=0, must-revalidate\\n')\"",
    "preview": "vite preview"
  },
  "dependencies": {
    "globe.gl": "^2.41.1",
    "topojson-client": "^3.1.0",
    "world-atlas": "^2.0.2"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
