{
  "name": "hte-threat-monitor",
  "version": "0.1.0",
  "private": true,
  "description": "Real-time Cyber Threat & Tech Monitoring Dashboard — zero-cost stack (Ollama + Upstash/In-memory cache + free RSS/OTX sources + globe.gl)",
  "type": "module",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "refresh": "npm run refresh -w backend",
    "build": "npm run build -w frontend",
    "start": "npm run start -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "esbuild@0.25.12": true
  }
}
